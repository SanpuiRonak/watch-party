networks:
  watchparty-network:
    driver: bridge

services:
  app:
    build: .
    expose:
      - "3000"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - SERVER_PORT=3000
      - MONGO_USER=${MONGO_USER:-admin}
      - MONGO_PASSWORD=${MONGO_PASSWORD:-changeme}
      - MONGO_HOST=mongodb
      - MONGO_PORT=27017
      - MONGODB_DB=${MONGODB_DB:-watchparty}
    depends_on:
      mongodb:
        condition: service_started
    networks:
      - watchparty-network

  mongodb:
    image: mongo:7
    expose:
      - "27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_USER:-admin}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASSWORD:-changeme}
      - MONGO_INITDB_DATABASE=${MONGODB_DB:-watchparty}
    volumes:
      - mongo-data:/data/db
    networks:
      - watchparty-network

volumes:
  mongo-data:
