# Application Environment
NODE_ENV=development
PORT=3000

# MongoDB Configuration
# IMPORTANT: Change these credentials for production!
MONGO_USER=admin
MONGO_PASSWORD=your-strong-password-here
MONGO_HOST=localhost
MONGO_PORT=27017
MONGODB_DB=watchparty



# Next.js Public Variables
NEXT_PUBLIC_SOCKET_URL=http://localhost:3000

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 1. Copy this file to .env and update with your actual credentials
# 2. NEVER commit .env to version control (.env is in .gitignore)
# 3. Use strong, randomly generated passwords in production
# 4. Generate strong passwords: openssl rand -base64 32
# 5. For production, use secrets management (AWS Secrets Manager, etc.)
