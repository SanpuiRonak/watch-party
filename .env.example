# ============================================================================
# MANDATORY CONFIGURATION - Required for the application to run
# ============================================================================
# Copy this file to .env and uncomment + configure the required variables below

# JWT Configuration
# CRITICAL: Generate a strong secret for production!
# Generate with: openssl rand -base64 64
JWT_SECRET=your-jwt-secret-change-in-production

# Next.js Public Variables
# The origin URL where your application is hosted (used for Socket.IO connection and CORS)
NEXT_PUBLIC_ORIGIN=http://localhost:3000


# ============================================================================
# OPTIONAL CONFIGURATION - Has sensible defaults, uncomment to customize
# ============================================================================

# Application Environment
# NODE_ENV=development
# PORT=3000

# MongoDB Configuration
# Note: Authentication is disabled - MongoDB only accessible from internal Docker network
# MONGO_HOST=localhost
# MONGO_PORT=27017
# MONGODB_DB=watchparty_dev
# TODO: Add MONGO_USER and MONGO_PASSWORD when re-enabling authentication (see mongodb.ts)

# CORS Configuration
# By default, NEXT_PUBLIC_ORIGIN is used as the allowed origin for Socket.IO CORS
# Only set ALLOWED_ORIGINS if you need multiple frontends to connect to the same backend
# Comma-separated list of allowed origins for production
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://admin.yourdomain.com,https://mobile.yourdomain.com
# ALLOWED_ORIGINS=
